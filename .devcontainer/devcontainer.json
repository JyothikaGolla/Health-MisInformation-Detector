{
  "name": "health-misinfo-detector",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "postCreateCommand": "pip install -r api/requirements.txt && cd client && npm i",
  "forwardPorts": [
    8000,
    5173
  ],
  "remoteUser": "root"
}